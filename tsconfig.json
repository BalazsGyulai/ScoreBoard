{
  "$comment": "Root tsconfig — only here for editor support across the whole monorepo. Each app has its own tsconfig.json. Do NOT add app-specific paths here.",
  "files": [],
  "references": [
    { "path": "./apps/web" }
  ]
}
